import 'package:flutter/material.dart';

/// Dana's 2차 화면 Figma 색상.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0046FF);
  static const Color primaryDark = Color(0xFF001BB7);
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);

  /// Note/강조 배경 (Figma)
  static const Color noteWarning = Color(0xFFFFC5BD);
  static const Color noteSuccess = Color(0xFF99E5B2);
  static const Color noteInfo = Color(0xFFE6D1FF);
}
